# Multi-node cluster: 1 control-plane node + 2 worker nodes.
# Use: kind create cluster --name k8s-practice --config kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
